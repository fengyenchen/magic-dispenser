{
  "scripts": {
    "dev": "concurrently -n \"VITE,EXPR\" -c \"blue,green\" \"npm:dev:frontend\" \"npm:dev:backend\"",
    "dev:frontend": "vite",
    "dev:backend": "cd server && npm run dev",
    "build": "tsc -b && vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  }
}